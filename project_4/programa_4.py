# Instrucciones para crear un proyecto con Django
print ("Para usar django hay que instalar un entorno virtual para no tener que instalar django en cada proyecto")
print ("El comando para instalarlo es :")
print ("pip3 install virtualenv")
print ("Para crear nuestro entorno virtual usamos")
print ("virtualenv -p nombreEntorno env")
print ("Se creara la carpeta con nuestro entorno virtual para activarlo ponemos")
print ("source env/bin/activate")
print ("con esto veremos que se activo entre parentesis (env)")
